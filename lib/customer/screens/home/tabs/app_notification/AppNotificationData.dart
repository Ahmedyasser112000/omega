part of 'NotificationImports.dart';

class AppNotificationData {
  // final GenericBloc<List<NotifyModel>> notifyBloc = new GenericBloc([]);
  // final GenericBloc<OrderDetailsModel?> chatCubit = new GenericBloc(null);
  //
  // fetchData(BuildContext context, {bool refresh = true}) async {
  //   var data = await CustomerRepository(context).getNotify(refresh);
  //   notifyBloc.onUpdateData(data);
  // }

  // navigateToChat(
  //   BuildContext context,
  //   int orderId,
  //   int index,
  // ) {
  //   var userType = context.read<UserCubit>().state.model.type;
  //
  //   if (notifyBloc.state.data[index].orderType == 1) {
  //     AutoRouter.of(context).push(
  //       ChatRoomRoute(
  //         firstChat: false,
  //         type: userType,
  //         orderId: orderId,
  //       ),
  //     );
  //   } else if (notifyBloc.state.data[index].orderType == 2 && userType == 2) {
  //     AutoRouter.of(context).push(ProCommentsRoute());
  //   } else if (notifyBloc.state.data[index].type == 7) {
  //     CustomToast.showSimpleToast(msg: tr(context, "fromDashboard"));
  //   }
  // }
}
